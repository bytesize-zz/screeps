/*
module.exports = {
    
    transporter: if workroom != homeroom && inhomeroom refill only storage (may reduce slaveTransporter need)
                also try increased energy usage for slaveTransporter

    claimer: don't need energy to clain/reserve - remove slave Container - and claimer behavior to refill ( new role? )

};

